server:
  port: 1112

eureka:
  instance:
    hostname: peer2

    client:
      #register-with-eureka: false
      #fetch-registry: false
      serviceUrl:
        defaultZone: http://localhost:1111/eureka/